# Numflow Blog - Feature-First Showcase

> **Numflow의 Feature-First 아키텍처를 활용한 포괄적인 설치형 블로그 시스템**

[![Tests](https://img.shields.io/badge/tests-188%20passing-brightgreen)](https://github.com)
[![Numflow](https://img.shields.io/badge/Numflow-0.2.x-blue)](https://github.com/gazerkr/numflow)
[![License](https://img.shields.io/badge/license-MIT-green)](LICENSE)

[English](README.md) | **한국어**

## 🎯 프로젝트 소개

이 프로젝트는 **Numflow 프레임워크의 공식 Showcase**입니다. Numflow의 핵심 기능인 **Feature-First 자동 오케스트레이션**을 실무에서 어떻게 활용하는지 보여주는 완전한 기능의 **설치형 블로그 시스템**입니다.

품질과 유지보수성에 중점을 두고 개발되었으며, Numflow의 가능성을 탐색하는 개발자들을 위한 **학습 자료**이자 **참고 구현**으로 활용할 수 있습니다.

## 🚀 Numflow란?

**Numflow**는 Express 5.x와 완전히 호환되면서도 최적화된 라우팅을 통해 향상된 성능(벤치마크 기준 최대 3배)을 제공하는 Node.js 웹 프레임워크입니다.

### Numflow의 핵심 강점

- ⚡ **고성능**: 최적화된 라우팅으로 Express 대비 3배 빠른 처리 속도 제공
- 🔄 **Express 호환성**: Express 5.x 미들웨어 및 패턴과 호환
- 🎨 **Feature-First 아키텍처**: 폴더 구조와 컨벤션을 통한 비즈니스 로직 구성
- 📦 **컨벤션 우선 설정**: 합리적인 기본값을 통한 보일러플레이트 감소
- 🧩 **모듈성**: 기능별 명확한 분리와 재사용성

👉 **Numflow 공식 저장소**: [github.com/gazerkr/numflow](https://github.com/gazerkr/numflow)

## ✨ Feature-First 아키텍처란?

Numflow의 **핵심 기능**으로, 폴더 구조와 파일명 규칙을 통해 복잡한 비즈니스 로직을 자동으로 오케스트레이션하는 아키텍처입니다.

### 작동 원리

```
features/admin/posts/@post/steps/
├── 100-auth.js          # 1단계: 인증 확인
├── 200-validate.js      # 2단계: 데이터 검증
├── 300-create-post.js   # 3단계: 게시물 생성
└── 400-redirect.js      # 4단계: 리다이렉트
```

위와 같은 폴더 구조만 만들면:

- ✅ `POST /admin/posts` 엔드포인트가 자동 등록
- ✅ 4개의 step이 100 → 200 → 300 → 400 순서로 자동 실행
- ✅ 각 step 간 데이터는 `ctx` 객체로 자동 공유
- ✅ **설정 파일이나 라우터 등록 코드 불필요!**

### 핵심 장점

#### 1️⃣ **향상된 생산성**

- 폴더 생성을 통한 다수의 엔드포인트 등록
- 라우터 설정 및 미들웨어 체이닝 보일러플레이트 감소
- 비즈니스 로직 구현에 집중

#### 2️⃣ **명확한 관심사 분리**

- HTTP 계층(`req`, `res`)과 비즈니스 로직(`ctx`) 분리
- 각 step은 단일 책임 수행
- 테스트 및 유지보수 용이

#### 3️⃣ **직관적인 구조**

- 폴더 구조가 API 구조를 반영
- 파일 번호가 실행 순서를 나타냄
- 폴더 구성만으로 흐름 파악 가능

#### 4️⃣ **유연한 설정**

- 필요시 `index.js`로 명시적 설정 추가 가능
- 컨벤션 우선, 필요시 명시적 설정

## 🎨 이 프로젝트가 보여주는 Numflow의 실전 활용

### 1. 복잡한 다단계 비즈니스 로직

**게시물 생성 프로세스** (`features/admin/posts/@post/`)

```
steps/
├── 100-auth.js        # 인증 확인
├── 200-validate.js    # 제목/내용 검증
├── 300-create-post.js # DB 저장 + 슬러그 생성
└── 400-redirect.js    # 성공 페이지 이동
```

→ **4개의 독립적인 파일**이 자동으로 순차 실행되며, 설정 코드는 **단 한 줄도 없음**

### 2. RESTful API 구현

```
features/admin/posts/
├── @get/              → GET /admin/posts (목록)
├── @post/             → POST /admin/posts (생성)
├── [id]/@get/         → GET /admin/posts/:id (조회)
├── [id]/@put/         → PUT /admin/posts/:id (수정)
└── [id]/@delete/      → DELETE /admin/posts/:id (삭제)
```

→ **폴더 5개**로 전체 CRUD 기능 구현

### 3. 동적 라우팅

`[id]`, `[slug]` 폴더명으로 동적 파라미터 자동 인식:

- `features/blog/[slug]/@get/` → `GET /blog/:slug`
- `features/api/comments/[id]/@delete/` → `DELETE /api/comments/:id`

### 4. 컨텍스트 기반 데이터 흐름

```javascript
// 100-validate.js
export default async (ctx, req, res) => {
  ctx.validatedData = { title, content } // ctx에 저장
}

// 200-create-post.js
export default async (ctx, req, res) => {
  const post = await prisma.post.create({
    data: ctx.validatedData  // ctx에서 사용
  })
  ctx.createdPost = post
}

// 300-redirect.js
export default async (ctx, req, res) => {
  res.redirect(`/admin/posts/${ctx.createdPost.id}`)
}
```

→ HTTP 계층과 비즈니스 로직의 **명확한 분리**

### 5. 완성도 있는 구현

- ✅ **188개 통합 테스트**로 핵심 기능 검증
- ✅ **Prisma ORM**을 통한 타입 안전 데이터베이스 작업
- ✅ **세션 기반 인증** 및 권한 관리
- ✅ **마크다운** 에디터와 렌더링
- ✅ **이미지 업로드** 기능
- ✅ **전문 검색** 기능
- ✅ XSS 및 SQL Injection **방지 조치**

## 🏗️ 주요 기능

### 📝 블로그 핵심 기능

- **게시물 관리**: 마크다운 에디터, 커버 이미지, 발행/임시저장
- **카테고리 & 태그**: 계층적 분류 및 필터링
- **댓글 시스템**: 실시간 댓글 작성 및 관리
- **전문 검색**: 제목, 내용, 발췌문 검색
- **페이지네이션**: 대용량 데이터 효율 처리

### 🔐 인증 & 권한

- **비밀번호 보안**: bcrypt 기반 해싱
- **세션 관리**: Express 세션 통합
- **역할 기반 접근**: Admin, Editor, Viewer 역할
- **설치 프로세스**: 최초 실행 시 안내형 설정

### 🎨 관리자 패널

- **대시보드**: 통계 및 최근 게시물 개요
- **컨텐츠 관리**: 게시물, 카테고리, 태그 관리
- **설정**: 블로그 설정 및 사용자 프로필
- **미디어**: 이미지 업로드 처리

### ⚡ 기술 스택

- **Numflow**: Express 호환 고성능 프레임워크
- **Prisma ORM**: 타입 안전 데이터베이스
- **SQLite**: 설정 불필요 임베디드 DB
- **EJS**: 서버 사이드 템플릿
- **Vitest**: 188개 통합 테스트

## 📖 학습 가이드

이 프로젝트를 통해 다음을 학습할 수 있습니다:

### 1. Feature-First 기본 패턴

→ `features/install/` 폴더 참조

- 가장 단순한 구조로 Feature-First 이해
- `@get`, `@post` 메서드 폴더 사용법
- 순차적 steps 실행 흐름

### 2. 다단계 비즈니스 로직

→ `features/admin/posts/@post/` 폴더 참조

- 인증 → 검증 → 생성 → 응답의 4단계 처리
- 각 단계의 책임 분리 방법
- 에러 처리 및 조기 종료 패턴

### 3. RESTful API 설계

→ `features/admin/categories/` 폴더 참조

- CRUD 전체 엔드포인트 구현
- 동적 파라미터 처리 (`[id]` 폴더)
- 일관된 응답 구조

### 4. 복잡한 쿼리 처리

→ `features/blog/search/` 폴더 참조

- 검색어 파싱 및 검증
- Prisma 복잡 쿼리 작성
- 페이지네이션 구현

### 5. 파일 업로드

→ `features/api/upload/image/` 폴더 참조

- 멀티파트 폼 데이터 처리
- 파일 검증 및 저장
- 에러 핸들링

## 🚀 빠른 시작

### 1. 설치

```bash
# 저장소 클론
git clone https://github.com/gazerkr/numflow-feature-first-blog.git
cd numflow-feature-first-blog

# 의존성 설치
npm install
```

### 2. 환경 설정

```bash
# .env 파일 생성
cp .env.example .env
```

`.env` 파일 수정:

```env
PORT=5555
NODE_ENV=development
APP_NAME="Numflow Blog"
DATABASE_URL="file:./dev.db"
SESSION_SECRET="change-this-to-random-string"
```

### 3. 데이터베이스 초기화

```bash
# Prisma 클라이언트 생성
npx prisma generate

# 데이터베이스 마이그레이션
npx prisma db push

# (선택) 샘플 데이터
npx prisma db seed
```

### 4. 실행

```bash
# 개발 모드
npm run dev

# 프로덕션 모드
npm start
```

### 5. 접속 및 설치

1. 브라우저에서 `http://localhost:5555` 접속
2. 자동으로 설치 페이지로 이동
3. 관리자 정보 입력 (사용자명, 이메일, 비밀번호)
4. 설치 완료 후 로그인
5. `/admin`에서 블로그 관리 시작!

## 📁 프로젝트 구조 (Feature-First)

```
features/                       # Feature-First 구조
│
├── install/                    # 설치 기능
│   ├── @get/steps/
│   │   └── 100-check.js       # 설치 여부 확인
│   └── @post/steps/
│       ├── 100-validate.js    # 입력 검증
│       ├── 200-create-admin.js # 관리자 생성
│       └── 300-mark-installed.js # 설치 완료 표시
│
├── auth/                       # 인증 기능
│   ├── login/@post/steps/
│   │   ├── 100-validate.js
│   │   ├── 200-authenticate.js
│   │   └── 300-create-session.js
│   └── logout/@post/steps/
│       └── 100-destroy-session.js
│
├── admin/                      # 관리자 패널
│   ├── @get/steps/            # 대시보드
│   │   ├── 100-auth.js
│   │   ├── 200-fetch-stats.js
│   │   └── 300-render.js
│   │
│   ├── posts/                  # 게시물 관리
│   │   ├── @get/              # 목록
│   │   ├── @post/             # 생성
│   │   ├── [id]/@put/         # 수정
│   │   └── [id]/@delete/      # 삭제
│   │
│   ├── categories/             # 카테고리 관리
│   └── tags/                   # 태그 관리
│
├── blog/                       # 블로그 공개 페이지
│   ├── @get/                  # 목록
│   ├── [slug]/@get/           # 상세 페이지
│   ├── category/[slug]/@get/  # 카테고리별
│   ├── tag/[slug]/@get/       # 태그별
│   └── search/@get/           # 검색
│
└── api/                        # API 엔드포인트
    ├── comments/
    ├── search/
    └── upload/
```

**→ 폴더 구조 = API 구조 = 실행 흐름**

## 🧪 테스트

```bash
# 전체 테스트 (188개)
npm test

# 특정 기능 테스트
npm test -- tests/features/install.test.js

# 커버리지
npm run test:coverage
```

**테스트 현황**: ✅ 26개 파일, 188개 테스트 모두 통과

## 📚 상세 문서

- [01-overview.md](docs/ko/01-overview.md) - 프로젝트 전체 개요
- [02-database-schema.md](docs/ko/02-database-schema.md) - 데이터베이스 스키마
- [03-features-and-pages.md](docs/ko/03-features-and-pages.md) - 기능 명세
- [04-feature-first-structure.md](docs/ko/04-feature-first-structure.md) - **Feature-First 아키텍처 상세**
- [05-authentication.md](docs/ko/05-authentication.md) - 인증 시스템

## 🎯 Numflow를 고려하는 이유

### ✅ 이 프로젝트가 보여주는 것들

1. **향상된 생산성**: Feature-First 아키텍처가 개발 워크플로우를 간소화
2. **유지보수성**: 폴더 구조를 통한 명확한 애플리케이션 흐름 파악
3. **확장성**: 일관된 패턴으로 새 폴더 생성을 통한 기능 추가
4. **테스트 용이성**: Step 기반 아키텍처로 집중적인 단위 테스트 가능
5. **팀 협업**: 기능 기반 분리로 병렬 개발 지원
6. **익숙한 생태계**: Express 개발자가 기존 지식 적용 가능

### 🚀 Express 사용자를 위한 고려사항

- Express 미들웨어 및 패턴과 **100% 호환**
- 최적화된 라우팅과 요청 처리를 통한 **성능 개선**
- 대규모 애플리케이션을 위한 **Feature-First 아키텍처** 제공
- Express에 익숙한 개발자를 위한 **낮은 학습 곡선**

## 🤝 기여하기

이 Showcase 프로젝트는 Numflow 생태계의 일부입니다.

- 🐛 버그 리포트: [Issues](https://github.com/gazerkr/numflow-feature-first-blog/issues)
- 💡 기능 제안: [Discussions](https://github.com/gazerkr/numflow-feature-first-blog/discussions)
- 🔧 Pull Request: 언제나 환영합니다!

## 🔗 관련 링크

- **Numflow 공식 저장소**: https://github.com/gazerkr/numflow
- **Feature-First 문서**: https://github.com/gazerkr/numflow/blob/main/docs/en/api/feature.md
- **Numflow 문서**: https://github.com/gazerkr/numflow/tree/main/docs
- **Prisma**: https://www.prisma.io
- **Vitest**: https://vitest.dev

## 📄 라이센스

MIT License - Numflow Showcase Project

---

<div align="center">

**이 프로젝트는 Numflow의 Feature-First 아키텍처를 실무에 적용한 예제입니다.**

⭐ 유용하다고 생각하신다면 [Numflow](https://github.com/gazerkr/numflow)에 GitHub Star를 부탁드립니다.

[Numflow](https://github.com/gazerkr/numflow)로 구축

</div>
