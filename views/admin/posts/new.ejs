<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New Post</title>
  <link rel="stylesheet" href="/css/styles.css">
  <%- include('../../partials/theme-script') %>
</head>
<body class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 dark:from-gray-800 dark:to-gray-900">
  <div class="max-w-4xl mx-auto px-4 py-8">
    <div class="bg-white dark:bg-gray-700 rounded-2xl shadow-2xl p-8">
      <h1 class="text-3xl font-bold text-gray-900 dark:text-white mb-8">New Post</h1>

      <% if (error) { %>
        <div class="bg-red-50 dark:bg-red-900/30 border border-red-200 dark:border-red-400 text-red-700 dark:text-red-300 px-4 py-3 rounded-lg mb-6">
          <%= error %>
        </div>
      <% } %>

      <form method="POST" action="/admin/posts" class="space-y-6">
        <!-- Title -->
        <div>
          <label for="title" class="block text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">
            Title <span class="text-red-500">*</span>
          </label>
          <input
            type="text"
            id="title"
            name="title"
            value="<%= formData.title || '' %>"
            required
            class="w-full px-4 py-3 border border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-white rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent transition"
          >
        </div>

        <!-- Content -->
        <div>
          <label for="content" class="block text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">
            Content (Markdown) <span class="text-red-500">*</span>
          </label>
          <textarea
            id="content"
            name="content"
            required
            rows="15"
            class="w-full px-4 py-3 border border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-white rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent transition font-mono text-sm"
          ><%= formData.content || '' %></textarea>
          <p class="text-sm text-gray-500 dark:text-gray-300 mt-2">You can use Markdown syntax.</p>
        </div>

        <!-- Image Upload -->
        <div>
          <label class="block text-sm font-semibold text-gray-700 mb-2">Upload Image</label>
          <input
            type="file"
            id="imageFile"
            accept="image/*"
            class="hidden"
          >
          <button
            type="button"
            id="imageUploadBtn"
            class="bg-gray-500 text-white px-6 py-2 rounded-lg hover:bg-gray-600 transition font-medium"
          >
            Choose Image
          </button>
          <span id="uploadStatus" class="ml-3 text-sm text-gray-600"></span>
          <p class="text-sm text-gray-500 mt-2">Uploaded images are automatically inserted in Markdown format.</p>
        </div>

        <!-- Category -->
        <div>
          <label for="categoryId" class="block text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2">
            Category
          </label>
          <select
            id="categoryId"
            name="categoryId"
            class="w-full px-4 py-3 border border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-white rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent transition"
          >
            <option value="">None</option>
            <% categories.forEach(category => { %>
              <option
                value="<%= category.id %>"
                <%= formData.categoryId === category.id ? 'selected' : '' %>
              >
                <%= category.name %>
              </option>
            <% }) %>
          </select>
        </div>

        <!-- Tags -->
        <div>
          <label class="block text-sm font-semibold text-gray-700 dark:text-gray-300 mb-3">Tags</label>
          <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-3 bg-gray-50 dark:bg-gray-900 p-4 rounded-lg">
            <% if (tags.length === 0) { %>
              <p class="text-sm text-gray-500 dark:text-gray-300 col-span-full">No tags available.</p>
            <% } else { %>
              <% tags.forEach(tag => { %>
                <label class="flex items-center space-x-2 cursor-pointer hover:bg-gray-100 dark:hover:bg-gray-600 p-2 rounded transition">
                  <input
                    type="checkbox"
                    name="tagIds"
                    value="<%= tag.id %>"
                    <%= (formData.tagIds || []).includes(tag.id) ? 'checked' : '' %>
                    class="w-4 h-4 text-indigo-600 rounded focus:ring-2 focus:ring-indigo-500"
                  >
                  <span class="text-sm text-gray-700 dark:text-gray-300"><%= tag.name %></span>
                </label>
              <% }) %>
            <% } %>
          </div>
        </div>

        <!-- Published -->
        <div>
          <label class="flex items-center space-x-3 cursor-pointer">
            <input
              type="checkbox"
              name="published"
              value="true"
              <%= formData.published ? 'checked' : '' %>
              class="w-5 h-5 text-indigo-600 rounded focus:ring-2 focus:ring-indigo-500"
            >
            <span class="text-sm font-semibold text-gray-700">Publish immediately</span>
          </label>
          <p class="text-sm text-gray-500 mt-2 ml-8">If unchecked, will be saved as draft.</p>
        </div>

        <!-- Actions -->
        <div class="flex items-center space-x-4 pt-6 border-t border-gray-200">
          <button
            type="submit"
            class="bg-gradient-to-r from-indigo-500 to-purple-600 text-white px-8 py-3 rounded-lg font-semibold hover:from-indigo-600 hover:to-purple-700 transform hover:-translate-y-0.5 transition-all shadow-md hover:shadow-lg"
          >
            Save Post
          </button>
          <a
            href="/admin/posts"
            class="bg-gray-500 text-white px-8 py-3 rounded-lg font-semibold hover:bg-gray-600 transition"
          >
            Cancel
          </a>
        </div>
      </form>
    </div>
  </div>

  <script>
    // Image upload functionality
    const imageUploadBtn = document.getElementById('imageUploadBtn')
    const imageFile = document.getElementById('imageFile')
    const contentTextarea = document.getElementById('content')
    const uploadStatus = document.getElementById('uploadStatus')

    // Open file picker when button clicked
    imageUploadBtn.addEventListener('click', () => {
      imageFile.click()
    })

    // Upload when file selected
    imageFile.addEventListener('change', async (e) => {
      const file = e.target.files[0]
      if (!file) return

      // Show uploading status
      uploadStatus.textContent = 'Uploading...'
      uploadStatus.style.color = '#667eea'

      try {
        // Create FormData
        const formData = new FormData()
        formData.append('image', file)

        // Call image upload API
        const response = await fetch('/api/upload/image', {
          method: 'POST',
          body: formData
        })

        const result = await response.json()

        if (result.success) {
          // Insert into textarea in Markdown format
          const imageMarkdown = `![${file.name}](${result.url})`
          const cursorPos = contentTextarea.selectionStart
          const textBefore = contentTextarea.value.substring(0, cursorPos)
          const textAfter = contentTextarea.value.substring(cursorPos)

          contentTextarea.value = textBefore + imageMarkdown + textAfter
          contentTextarea.selectionStart = cursorPos + imageMarkdown.length
          contentTextarea.selectionEnd = cursorPos + imageMarkdown.length
          contentTextarea.focus()

          // Success message
          uploadStatus.textContent = '✓ Upload complete'
          uploadStatus.style.color = '#28a745'

          // Remove message after 3 seconds
          setTimeout(() => {
            uploadStatus.textContent = ''
          }, 3000)
        } else {
          throw new Error(result.error || 'Upload failed')
        }
      } catch (error) {
        uploadStatus.textContent = `✗ Upload failed: ${error.message}`
        uploadStatus.style.color = '#dc3545'
      }

      // Reset file input
      imageFile.value = ''
    })
  </script>
</body>
</html>
