<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tag Management</title>
  <link rel="stylesheet" href="/css/styles.css">
  <%- include('../../partials/theme-script') %>
</head>
<body class="bg-gray-50 dark:bg-gray-900">
  <nav class="bg-gradient-to-r from-gray-800 to-gray-900 shadow-lg">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center justify-between h-16">
        <div class="flex items-center space-x-8">
          <a href="/admin" class="text-white hover:text-indigo-300 transition font-medium">Dashboard</a>
          <a href="/admin/posts" class="text-white hover:text-indigo-300 transition font-medium">Posts</a>
          <a href="/admin/categories" class="text-white hover:text-indigo-300 transition font-medium">Categories</a>
          <a href="/admin/tags" class="text-white hover:text-indigo-300 transition font-medium border-b-2 border-indigo-400 pb-1">Tags</a>
          <a href="/admin/settings" class="text-white hover:text-indigo-300 transition font-medium">Settings</a>
          <a href="/blog" class="text-white hover:text-indigo-300 transition font-medium">View Blog</a>
        </div>
        <div class="flex items-center space-x-4">
          <span class="text-white text-sm"><%= currentUser.displayName %></span>
          <form method="POST" action="/auth/logout" class="inline">
            <button type="submit" class="text-white hover:text-red-300 transition font-medium">Logout</button>
          </form>
        </div>
      </div>
    </div>
  </nav>

  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <div class="bg-white dark:bg-gray-700 rounded-xl shadow-lg p-8">
      <div class="flex justify-between items-center mb-8">
        <h1 class="text-3xl font-bold text-gray-900 dark:text-white">Tag Management</h1>
        <a href="/admin/tags/new" class="bg-gradient-to-r from-indigo-500 to-purple-600 text-white px-6 py-3 rounded-lg font-semibold hover:from-indigo-600 hover:to-purple-700 transform hover:-translate-y-0.5 transition-all shadow-md hover:shadow-lg">
          New Tag
        </a>
      </div>

      <% if (tags.length === 0) { %>
        <div class="text-center py-16">
          <svg class="mx-auto h-16 w-16 text-gray-400 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>
          </svg>
          <p class="text-xl text-gray-600 dark:text-gray-300 mb-6">No tags yet</p>
          <a href="/admin/tags/new" class="inline-block bg-gradient-to-r from-indigo-500 to-purple-600 text-white px-8 py-3 rounded-lg font-semibold hover:from-indigo-600 hover:to-purple-700 transform hover:-translate-y-0.5 transition-all shadow-md hover:shadow-lg">
            Create First Tag
          </a>
        </div>
      <% } else { %>
        <div class="overflow-x-auto">
          <table class="min-w-full divide-y divide-gray-200 dark:divide-gray-600">
            <thead class="bg-gray-50 dark:bg-gray-900">
              <tr>
                <th class="px-6 py-4 text-left text-xs font-semibold text-gray-700 dark:text-gray-300 uppercase tracking-wider">Name</th>
                <th class="px-6 py-4 text-left text-xs font-semibold text-gray-700 dark:text-gray-300 uppercase tracking-wider">Slug</th>
                <th class="px-6 py-4 text-left text-xs font-semibold text-gray-700 dark:text-gray-300 uppercase tracking-wider">Actions</th>
              </tr>
            </thead>
            <tbody class="bg-white dark:bg-gray-700 divide-y divide-gray-200 dark:divide-gray-600">
              <% tags.forEach(tag => { %>
                <tr class="hover:bg-gray-50 dark:hover:bg-gray-600 transition">
                  <td class="px-6 py-4 whitespace-nowrap">
                    <span class="inline-flex px-3 py-1 text-sm font-semibold rounded-full bg-indigo-100 text-indigo-800">
                      <%= tag.name %>
                    </span>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-600 dark:text-gray-300"><%= tag.slug %></td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm">
                    <form method="POST" action="/admin/tags/<%= tag.id %>?_method=DELETE" class="inline">
                      <button type="submit" onclick="return confirm('Are you sure you want to delete this?')" class="bg-red-500 text-white px-4 py-2 rounded-md hover:bg-red-600 transition font-medium">
                        Delete
                      </button>
                    </form>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>
