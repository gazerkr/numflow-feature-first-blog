<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Administrator Dashboard - Numflow Blog</title>
  <link rel="stylesheet" href="/css/styles.css">
  <%- include('../partials/theme-script') %>
</head>
<body class="bg-gray-50 dark:bg-gray-900">
  <header class="bg-gray-800 dark:bg-gray-900 text-white shadow-md">
    <div class="px-8 py-6">
      <h1 class="text-3xl font-bold mb-2">Administrator Dashboard</h1>
      <div class="text-sm opacity-90">
        Logged in as <%= currentUser.displayName %>
      </div>
    </div>
  </header>

  <nav class="bg-gray-700 dark:bg-gray-800">
    <div class="px-8 py-4 flex items-center gap-8">
      <a href="/admin" class="text-white hover:text-indigo-300 dark:hover:text-indigo-400 transition no-underline">Dashboard</a>
      <a href="/admin/posts" class="text-white hover:text-indigo-300 dark:hover:text-indigo-400 transition no-underline">Posts</a>
      <a href="/admin/categories" class="text-white hover:text-indigo-300 dark:hover:text-indigo-400 transition no-underline">Categories</a>
      <a href="/admin/tags" class="text-white hover:text-indigo-300 dark:hover:text-indigo-400 transition no-underline">Tags</a>
      <a href="/admin/settings" class="text-white hover:text-indigo-300 dark:hover:text-indigo-400 transition no-underline">Settings</a>
      <a href="/blog" class="text-white hover:text-indigo-300 dark:hover:text-indigo-400 transition no-underline">View Blog</a>
      <form method="POST" action="/auth/logout" class="inline ml-8">
        <button type="submit" class="bg-transparent border-none text-white hover:text-indigo-300 dark:hover:text-indigo-400 cursor-pointer transition no-underline">
          Logout
        </button>
      </form>
    </div>
  </nav>

  <div class="max-w-7xl mx-auto px-8 py-8">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-8">
      <div class="bg-white dark:bg-gray-700 p-8 rounded-lg shadow-lg text-center">
        <h3 class="text-gray-500 dark:text-gray-300 text-sm font-medium mb-4 uppercase tracking-wider">Total Posts</h3>
        <div class="text-5xl font-bold text-gray-900 dark:text-white"><%= stats.totalPosts %></div>
      </div>
      <div class="bg-white dark:bg-gray-700 p-8 rounded-lg shadow-lg text-center">
        <h3 class="text-gray-500 dark:text-gray-300 text-sm font-medium mb-4 uppercase tracking-wider">Published Posts</h3>
        <div class="text-5xl font-bold text-gray-900 dark:text-white"><%= stats.publishedPosts %></div>
      </div>
      <div class="bg-white dark:bg-gray-700 p-8 rounded-lg shadow-lg text-center">
        <h3 class="text-gray-500 dark:text-gray-300 text-sm font-medium mb-4 uppercase tracking-wider">Total Views</h3>
        <div class="text-5xl font-bold text-gray-900 dark:text-white"><%= stats.totalViews %></div>
      </div>
    </div>

    <div class="bg-white dark:bg-gray-700 p-8 rounded-lg shadow-lg">
      <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-6 pb-2 border-b-2 border-gray-200 dark:border-gray-600">Recent Posts</h2>

      <% if (recentPosts.length === 0) { %>
        <div class="text-center py-12">
          <p class="text-lg text-gray-500 dark:text-gray-300">No posts yet</p>
        </div>
      <% } else { %>
        <ul class="divide-y divide-gray-200 dark:divide-gray-600">
          <% recentPosts.forEach(post => { %>
            <li class="py-4 flex justify-between items-center">
              <span class="flex-1 font-medium text-gray-900 dark:text-white"><%= post.title %></span>
              <span class="ml-4 px-3 py-1 rounded text-sm font-medium <%= post.published ? 'bg-green-100 dark:bg-green-900 text-green-800 dark:text-green-200' : 'bg-yellow-100 dark:bg-yellow-900 text-yellow-800 dark:text-yellow-200' %>">
                <%= post.published ? 'Published' : 'draft' %>
              </span>
              <div class="flex gap-2 ml-4">
                <a href="/admin/posts/<%= post.id %>/edit" class="px-4 py-2 bg-indigo-500 dark:bg-indigo-600 text-white rounded hover:bg-indigo-600 dark:hover:bg-indigo-700 transition text-sm no-underline">Edit</a>
                <form method="POST" action="/admin/posts/<%= post.id %>/delete" class="inline" onsubmit="return confirm('Are you sure you want to delete this?')">
                  <button type="submit" class="px-4 py-2 bg-red-500 dark:bg-red-600 text-white rounded hover:bg-red-600 dark:hover:bg-red-700 transition text-sm border-none cursor-pointer">Delete</button>
                </form>
              </div>
            </li>
          <% }) %>
        </ul>
      <% } %>

      <div class="mt-8">
        <a href="/admin/posts/new" class="inline-block px-6 py-3 bg-indigo-500 dark:bg-indigo-600 text-white rounded-lg hover:bg-indigo-600 dark:hover:bg-indigo-700 transition no-underline font-medium">New Post</a>
      </div>
    </div>
  </div>
</body>
</html>
